spring.application.name=SocialMedia
# MySQL Datasource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/socialmedia_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root_admin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Secret Key
jwt.secret=ahfkahfiahifjbwqeiiquhr92183y9817293h12ir8t1hiwhdfiadhfiah

# Other optional settings
server.port=8080
logging.level.org.springframework.security=DEBUG

#all validations
validation.email.regex=^[a-zA-Z0-9][a-zA-Z0-9._%+-]*[a-zA-Z0-9]@[a-zA-Z0-9][a-zA-Z0-9.-]*\\.[a-zA-Z]{2,}$
validation.email.message=Invalid email address

validation.username.regex=^[a-zA-Z0-9][a-zA-Z0-9._-]{1,28}[a-zA-Z0-9]$
validation.username.message=Username must be 3-30 characters, start and end with a letter or number, and contain only letters, numbers, underscores, periods, or hyphens

validation.password.regex=^[^\s]{8,100}$
validation.password.message=Password must be 8-100 characters with no spaces