spring.application.name=SocialMedia
# MySQL Datasource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/socialmedia_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root  # Replace with your MySQL username
spring.datasource.password=root_admin  # Replace with your MySQL password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update  # Options: create, update, validate, none (use 'update' for dev to auto-sync entities)
spring.jpa.show-sql=true  # Logs SQL queries
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Secret Key
jwt.secret=ahfkahfiahifjbwqeiiquhr92183y9817293h12ir8t1hiwhdfiadhfiah

# Other optional settings
server.port=8080
logging.level.org.springframework.security=DEBUG  # For debugging auth issues